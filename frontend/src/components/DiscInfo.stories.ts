import type { Meta, StoryObj } from "@storybook/vue3";
import DiscInfo from "../components/DiscInfo.vue";

const meta: Meta<typeof DiscInfo> = {
  component: DiscInfo,
}

export default meta;
type Story = StoryObj<typeof DiscInfo>;

export const Primary: Story = {
};
